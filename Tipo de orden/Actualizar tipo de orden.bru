meta {
  name: Actualizar tipo de orden
  type: http
  seq: 4
}

put {
  url: http://localhost:8080/api/v1/tipossorden/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{LLAVE JWT}}
}

body:json {
  {
      "nombre" : "Delivery"
  }
}
