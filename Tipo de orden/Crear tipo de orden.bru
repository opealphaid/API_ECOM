meta {
  name: Crear tipo de orden
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/v1/tipossorden
  body: json
  auth: bearer
}

auth:bearer {
  token: {{LLAVE JWT}}
}

body:json {
  {
      "nombre" : "Delivery"
  }
}
